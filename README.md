# http-server-info

## Purpose

Reliably fetches a ROBLOX server's geographic location via the IP-API

## Usage
- Copy the script's content in the /src file and create a new script into your ServerScriptStorage with the content
- By default, your workspace gets a Region attribute with something like "Hesse, Germany"
- There is more inside the "serverInfo" variable, check it out